import { TypeDeHebergement } from './type-de-hebergement';

describe('TypeDeHebergement', () => {
  it('should create an instance', () => {
    expect(new TypeDeHebergement()).toBeTruthy();
  });
});
